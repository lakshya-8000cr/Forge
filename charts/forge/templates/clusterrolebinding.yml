apiVersion: rbac.authorization.k8s.io/v1

kind: ClusterRoleBinding

metadata:

  name: forge-helm-binding

subjects:

- kind: ServiceAccount

  name: forge-backend-sa

  namespace: default

roleRef:

  kind: ClusterRole

  name: forge-helm-role

  apiGroup: rbac.authorization.k8s.io