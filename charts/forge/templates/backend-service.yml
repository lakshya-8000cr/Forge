apiVersion: v1
kind: Service

metadata:
  name: forge-backend

spec:
  type: ClusterIP

  selector:
    app: forge-backend

  ports:
    - port: {{ .Values.backend.port }}
      targetPort: {{ .Values.backend.port }}